--- a/webview/assets/index-CAbvp5mX.js
+++ b/webview/assets/index-CAbvp5mX.js
@@ -18412,6 +18412,7 @@
   let r = Me(Wo),
     i = f(),
     a = J(sr, e),
+    codexHostedWindowTitle = J(ya, e),
     o = J(mn, e),
     s = J(It, e) ?? A5,
     c = J(_r, e) ?? k5,
@@ -18448,7 +18449,13 @@
   ),
     (0, Z.useEffect)(() => {
       E?.id == null || E.readAt != null || w(E.id);
-    }, [E?.id, E?.readAt, w]));
+    }, [E?.id, E?.readAt, w]),
+    (0, Z.useEffect)(() => {
+      let threadTitleForWindow = codexHostedWindowTitle?.trim();
+      document.title = threadTitleForWindow
+        ? `${threadTitleForWindow} | Codex`
+        : `Codex`;
+    }, [codexHostedWindowTitle]));
   let k = Ni(),
     { agentMode: A } = wc({ conversationId: e }),
     j = s.some((e) => e.items.some((e) => e.type === `userMessage`)),
