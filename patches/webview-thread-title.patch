--- a/webview/assets/index-Dcyrtuk-.js
+++ b/webview/assets/index-Dcyrtuk-.js
@@ -121050,6 +121050,7 @@
   let i = R(mh),
     a = d(),
     o = z(ur, e),
+    codexHostedWindowTitle = z(ga, e),
     s = z(Cn, e),
     c = z(gn, e) ?? x5,
     l = z(wr, e),
@@ -121100,7 +121101,11 @@
   ),
     (0, Z.useEffect)(() => {
       M?.id == null || M.readAt != null || j(M.id);
-    }, [M?.id, M?.readAt, j]));
+    }, [M?.id, M?.readAt, j]),
+    (0, Z.useEffect)(() => {
+      let t = codexHostedWindowTitle?.trim();
+      t && (document.title = `${t} | Codex`);
+    }, [codexHostedWindowTitle]));
   let F = Da(),
     { agentMode: I } = mg({ conversationId: e, hostId: u }),
     L = l ? De(l) : null,
