--- a/webview/assets/index-1LJShyXg.js
+++ b/webview/assets/index-1LJShyXg.js
@@ -120573,6 +120573,7 @@
   let r = He(Cm),
     i = d(),
     a = We(pr, e),
+    codexHostedWindowTitle = We(ga, e),
     o = We(Tr, e),
     s = We(on, e) ?? O5,
     c = We(ni, e),
@@ -120623,7 +120624,11 @@
   ),
     (0, Z.useEffect)(() => {
       j?.id == null || j.readAt != null || A(j.id);
-    }, [j?.id, j?.readAt, A]));
+    }, [j?.id, j?.readAt, A]),
+    (0, Z.useEffect)(() => {
+      let t = codexHostedWindowTitle?.trim();
+      t && (document.title = `${t} | Codex`);
+    }, [codexHostedWindowTitle]));
   let P = ea(),
     { agentMode: F } = If({ conversationId: e, hostId: l }),
     I = c ? ve(c) : null,
