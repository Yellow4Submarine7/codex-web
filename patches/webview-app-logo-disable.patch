--- a/webview/assets/use-model-settings-Ds_s4Jfi.js
+++ b/webview/assets/use-model-settings-Ds_s4Jfi.js
@@ -24971,41 +24971,7 @@ function wx(e) {
   );
 }
 function Tx(e) {
-  let t = (0, Y.c)(8),
-    n;
-  t[0] === e
-    ? (n = t[1])
-    : ((n = e === void 0 ? {} : e), (t[0] = e), (t[1] = n));
-  let { enabled: r, hostId: i } = n,
-    a = r === void 0 ? !0 : r,
-    o;
-  t[2] !== a || t[3] !== i
-    ? ((o = { enabled: a, hostId: i }), (t[2] = a), (t[3] = i), (t[4] = o))
-    : (o = t[4]);
-  let s = wx(o),
-    c = a ? (s.data ?? []) : [],
-    l = Lx(c),
-    u = gn({ queries: l.map(Ex) }),
-    d = new Map();
-  l.forEach((e, t) => {
-    let n = u[t];
-    n?.data != null && d.set(hx(e), n.data);
-  });
-  let f = c.map((e) => {
-    let t = Rx({ logoUrl: e.logoUrl, connectorLogoDataUrlByCacheKey: d }),
-      n = Rx({ logoUrl: e.logoUrlDark, connectorLogoDataUrlByCacheKey: d });
-    return t === e.logoUrl && n === e.logoUrlDark
-      ? e
-      : { ...e, logoUrl: t, logoUrlDark: n };
-  });
-  if (!a || s.data == null) return s;
-  let p;
-  return (
-    t[5] !== s || t[6] !== f
-      ? ((p = { ...s, data: f }), (t[5] = s), (t[6] = f), (t[7] = p))
-      : (p = t[7]),
-    p
-  );
+  return { data: [] };
 }
 function Ex(e) {
   return {
