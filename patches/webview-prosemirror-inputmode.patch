--- a/webview/assets/use-model-settings-BXpE6yHZ.js
+++ b/webview/assets/use-model-settings-BXpE6yHZ.js
@@ -54298,6 +54298,11 @@
   let o = new EventTarget(),
     s = new ble(),
     c = a,
+    lre = !1,
+    ure = () => (lre ? {} : { inputmode: `none` }),
+    dre = (e) => {
+      lre !== e && ((lre = e), l.isDestroyed || l.setProps({ attributes: ure }));
+    },
     l = new UM(null, {
       state: fE.create({
         schema: SN,
@@ -54375,6 +54380,18 @@
         let t = l.state.apply(e);
         (l.updateState(t), o.dispatchEvent(new Event(yE)));
       },
+      attributes: ure,
+      handleDOMEvents: {
+        mousedown(e, t) {
+          return dre(!0), !1;
+        },
+        touchstart(e, t) {
+          return dre(!0), !1;
+        },
+        blur(e, t) {
+          return dre(!1), !1;
+        },
+      },
       handlePaste(e, t) {
         if (t.defaultPrevented) return !0;
         let n = t.clipboardData,
